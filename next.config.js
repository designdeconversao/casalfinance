/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  experimental: {
    // If you need specific experimental options
  },
  // In Next.js 14/15, some options changed. 
  // The error in Vercel is specifically about the .ts extension.
};

module.exports = nextConfig;
